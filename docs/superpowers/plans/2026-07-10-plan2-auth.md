# Studio — Plan 2: 인증/사용자 (Auth) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 자율 회원가입 + 관리자 승인 방식의 사용자 인증을 구현한다. httpOnly 쿠키 기반 JWT 로그인(액세스+리프레시 토큰 회전), `current_user`/`require_admin` 의존성, 관리자 승인/거절 API, 최초 admin 시드까지 동작하는 실행 가능한 슬라이스를 만든다.

**Architecture:** Plan 1(Foundation) 위에 이어서 진행. 스키마(`SQLModel`)와 쿼리(`aiosql` `.sql` 파일)를 분리하는 기존 규칙을 그대로 따른다 — `User`/`RefreshToken`은 테이블 정의(Alembic 마이그레이션)에만 쓰고, 실제 조회/등록/수정은 `app/queries/*.sql` + `raw_connection(session)` 브릿지로 수행한다. 에러는 기존 `AppError`/`Errors`(소스 관리, DB 미사용)를 그대로 사용한다.

**Tech Stack:** 기존 스택(Python 3.12, uv, FastAPI, SQLModel/SQLAlchemy async, asyncpg, Alembic, aiosql, pytest+testcontainers) + 이번 Plan에서 추가하는 **argon2-cffi**(비밀번호 해시), **PyJWT**(액세스 토큰).

## Global Constraints

- 액세스 토큰 유효시간: **30분** (스펙 범위 15~30분 중 상한 채택).
- 리프레시 토큰 유효시간: **14일** (스펙 범위 7~14일 중 상한 채택), **DB에 해시로 저장**하고 사용할 때마다 회전(재사용 탐지 시 해당 사용자의 모든 리프레시 토큰 폐기).
- 비밀번호 해시: **argon2-cffi** (`PasswordHasher` 기본 파라미터). 평문 저장 금지.
- JWT: **PyJWT**, 알고리즘 `HS256`, 시크릿은 기존 `app.config.get_settings().jwt_secret`.
- **JWT의 `iat`/`exp`는 실제 UTC 절대시각**(`datetime.now(timezone.utc)`)이어야 한다 — PyJWT는 datetime 값을 UTC로 간주해 처리하므로, 프로젝트의 로컬시간 저장 규칙(`now_local()`, DB 컬럼용)과 섞어 쓰면 안 된다. DB에 저장하는 시각(`created_at`/`expires_at` 등)은 기존 규칙대로 `now_local()`을 그대로 쓴다.
- 쿠키: **httpOnly**, `SameSite=Lax`, `Secure`는 `.env`의 `SECURE_COOKIES` 설정을 그대로 따른다.
- 역할(`role`): `member` | `admin` (기본 `member`). 상태(`status`): `pending` | `active` | `disabled` | `rejected` (기본 `pending`).
- 로그인 실패 메시지는 계정 존재 여부와 관계없이 동일한 문구("이메일 또는 비밀번호가 올바르지 않습니다.")를 사용해 계정 열거를 방지한다.
- **Rate limiting은 이번 Plan 범위에서 제외**(후속 보안 강화 Plan으로 이월). 무차별 대입 방어는 이번 Plan에 포함하지 않는다.
- `created_by`/`updated_by`는 Plan 1에서 `users` 테이블 생성 후 FK를 걸기로 보류했던 부분 — 이번 Plan에서 `users.id` 자기참조 FK(nullable)로 실제 제약을 추가한다.
- 실제 DB 조회/등록/수정은 **ORM 쿼리빌더로 작성하지 않는다** — `app/queries/*.sql` + `aiosql` + `app.db.raw_connection(session)`으로만 수행한다(기존 규칙, `docs/superpowers/specs/2026-07-09-studio-design.md` "쿼리 관리" 절).
- API 에러 응답 포맷은 항상 `{"code": <str>, "message": <str>}` (기존 `AppError`/`Errors` 그대로 사용, 신규 에러 코드 추가 없음 — `Errors.unauthorized/forbidden/conflict/not_found`로 충분).
- 커밋은 각 Task 마지막 단계에서 수행(자주 커밋).

---

## File Structure

```
studio/
├─ pyproject.toml                # argon2-cffi, pyjwt 추가
├─ .env / .env.example           # ADMIN_EMAIL / ADMIN_PASSWORD 추가 (Task 9)
├─ app/
│  ├─ config.py                  # admin_email / admin_password 필드 추가 (Task 9)
│  ├─ main.py                    # auth/admin 라우터 등록
│  ├─ models/
│  │  ├─ __init__.py             # User, RefreshToken 재노출
│  │  ├─ user.py                 # User 테이블 (신규)
│  │  └─ refresh_token.py        # RefreshToken 테이블 (신규)
│  ├─ queries/
│  │  ├─ users.sql               # find_by_email, insert_user, find_by_id, list_by_status, update_status (신규)
│  │  └─ refresh_tokens.sql      # insert_refresh_token, find_by_token_hash, revoke_by_id, revoke_all_for_user (신규)
│  └─ auth/                      # 신규 패키지
│     ├─ __init__.py             # 빈 파일
│     ├─ security.py             # 비밀번호 해시 + JWT + 리프레시 토큰 유틸
│     ├─ dependencies.py         # current_user, require_admin
│     ├─ router.py               # /auth/register, /auth/login, /auth/refresh, /auth/logout
│     ├─ admin_router.py         # /admin/users, /admin/users/{id}/approve|reject
│     └─ seed_admin.py           # 최초 admin 시드 로직(핵심 함수, 테스트 가능)
├─ scripts/
│  └─ seed_admin.py              # CLI 진입점 (uv run python scripts/seed_admin.py)
├─ alembic/versions/             # users, refresh_tokens 마이그레이션 (autogenerate)
└─ tests/
   ├─ conftest.py                # client 픽스처 추가 (db_session 오버라이드 포함)
   ├─ test_user_model.py
   ├─ test_security.py
   ├─ test_refresh_token.py
   ├─ test_auth_register.py
   ├─ test_auth_login.py
   ├─ test_auth_refresh_logout.py
   ├─ test_auth_dependencies.py
   ├─ test_admin_users.py
   ├─ test_seed_admin.py
   └─ test_alembic_migration.py  # users/refresh_tokens 존재 검증 추가
```

각 파일 책임: `models/user.py`·`models/refresh_token.py`(스키마만), `queries/*.sql`(실제 쿼리), `auth/security.py`(암호화·토큰 순수 유틸), `auth/dependencies.py`(인가), `auth/router.py`/`admin_router.py`(HTTP 계층), `auth/seed_admin.py`(부트스트랩).

---

### Task 1: User 모델 & users 테이블 마이그레이션

**Files:**
- Create: `app/models/user.py`
- Modify: `app/models/__init__.py`
- Modify: `tests/test_alembic_migration.py`
- Test: `tests/test_user_model.py`

**Interfaces:**
- Consumes: `app.models.base.BaseEntity`.
- Produces: `app.models.user.User` (table=`users`). 컬럼: `email: str`(unique, index), `password_hash: str`, `role: str = "member"`, `status: str = "pending"`, `approved_at: datetime | None`, `approved_by: int | None`(FK `users.id`) + `BaseEntity`의 `id`/`created_at`/`updated_at`/`created_by`(FK `users.id`)/`updated_by`(FK `users.id`).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_user_model.py`:
```python
import pytest
from sqlalchemy.exc import IntegrityError

from app.models.user import User


async def test_user_defaults(db_session):
    user = User(email="a@example.com", password_hash="hashed-value")
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)

    assert isinstance(user.id, int)
    assert user.role == "member"
    assert user.status == "pending"
    assert user.approved_at is None
    assert user.approved_by is None


async def test_user_email_is_unique(db_session):
    db_session.add(User(email="dup@example.com", password_hash="h1"))
    await db_session.commit()

    db_session.add(User(email="dup@example.com", password_hash="h2"))
    with pytest.raises(IntegrityError):
        await db_session.commit()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_user_model.py -v`
Expected: FAIL — `ModuleNotFoundError: app.models.user`.

- [ ] **Step 3: app/models/user.py 구현**

```python
from datetime import datetime
from typing import Optional

from sqlalchemy import BigInteger
from sqlmodel import Field

from app.models.base import BaseEntity


class User(BaseEntity, table=True):
    __tablename__ = "users"

    email: str = Field(unique=True, index=True)
    password_hash: str
    role: str = Field(default="member")
    status: str = Field(default="pending")
    approved_at: Optional[datetime] = Field(default=None)
    approved_by: Optional[int] = Field(
        default=None, sa_type=BigInteger, foreign_key="users.id", nullable=True
    )
    created_by: Optional[int] = Field(
        default=None, sa_type=BigInteger, foreign_key="users.id", nullable=True
    )
    updated_by: Optional[int] = Field(
        default=None, sa_type=BigInteger, foreign_key="users.id", nullable=True
    )
```

> `created_by`/`updated_by`를 `BaseEntity`에서 그대로 물려받지 않고 여기서 다시 선언하는 이유: `BaseEntity`의 두 컬럼은 FK가 없는 순수 `BigInteger`다(여러 테이블이 공유하는 믹스인이라 특정 테이블을 가리킬 수 없음). `users` 테이블은 자기 자신을 가리키는 자기참조 FK가 필요하므로, 이 테이블에서만 같은 이름으로 재선언해 `foreign_key="users.id"`를 추가한다(타입은 `BaseEntity`와 동일하게 `sa_type=BigInteger` 유지).

`app/models/__init__.py`:
```python
from app.models.base import BaseEntity
from app.models.user import User

__all__ = ["BaseEntity", "User"]
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `uv run pytest tests/test_user_model.py -v`
Expected: PASS (2개 테스트).

- [ ] **Step 5: Alembic 마이그레이션 생성 & 적용**

로컬 Postgres가 기동 중이어야 한다(`docker compose up -d db`, 이미 Plan 1에서 구성됨).

Run: `uv run alembic revision --autogenerate -m "create users table"`
Expected: `alembic/versions/xxxx_create_users_table.py` 생성, `users` 테이블 create + FK 제약(`approved_by`/`created_by`/`updated_by` → `users.id`) 포함.

Run: `uv run alembic upgrade head`
Expected: 에러 없이 적용.

- [ ] **Step 6: 전체 마이그레이션 회귀 테스트에 users 테이블 검증 추가**

`tests/test_alembic_migration.py`의 마지막 assert 블록을 다음으로 교체:
```python
    assert "error_codes" not in tables
    assert "users" in tables
    assert "alembic_version" in tables
```

Run: `uv run pytest tests/test_alembic_migration.py -v`
Expected: PASS.

- [ ] **Step 7: 커밋**

```bash
git add app/models/user.py app/models/__init__.py tests/test_user_model.py tests/test_alembic_migration.py alembic/versions/
git commit -m "기능: User 모델 및 users 테이블 마이그레이션 추가 (자기참조 FK 포함)"
```

---

### Task 2: 보안 유틸 — 비밀번호 해시 + JWT 액세스 토큰

**Files:**
- Create: `app/auth/__init__.py` (빈 파일)
- Create: `app/auth/security.py`
- Test: `tests/test_security.py`

**Interfaces:**
- Consumes: `app.config.get_settings`.
- Produces:
  - `app.auth.security.ACCESS_TOKEN_MINUTES: int = 30`
  - `app.auth.security.hash_password(password: str) -> str`
  - `app.auth.security.verify_password(password: str, password_hash: str) -> bool`
  - `app.auth.security.create_access_token(user_id: int, role: str) -> str`
  - `app.auth.security.decode_access_token(token: str) -> dict` (실패 시 `jwt.InvalidTokenError` 서브클래스 발생)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_security.py`:
```python
from datetime import datetime, timedelta, timezone

import jwt
import pytest

from app.auth.security import (
    create_access_token,
    decode_access_token,
    hash_password,
    verify_password,
)
from app.config import get_settings


def test_hash_password_produces_verifiable_hash():
    hashed = hash_password("s3cret!")
    assert hashed != "s3cret!"
    assert verify_password("s3cret!", hashed) is True


def test_verify_password_rejects_wrong_password():
    hashed = hash_password("s3cret!")
    assert verify_password("wrong", hashed) is False


def test_create_and_decode_access_token_roundtrip():
    token = create_access_token(user_id=42, role="member")
    payload = decode_access_token(token)

    assert payload["sub"] == "42"
    assert payload["role"] == "member"


def test_decode_rejects_tampered_token():
    token = create_access_token(user_id=1, role="member")
    tampered = token[:-1] + ("A" if token[-1] != "A" else "B")

    with pytest.raises(jwt.InvalidTokenError):
        decode_access_token(tampered)


def test_decode_rejects_expired_token():
    past = datetime.now(timezone.utc) - timedelta(hours=1)
    expired_payload = {
        "sub": "1",
        "role": "member",
        "iat": past - timedelta(minutes=30),
        "exp": past,
    }
    token = jwt.encode(expired_payload, get_settings().jwt_secret, algorithm="HS256")

    with pytest.raises(jwt.InvalidTokenError):
        decode_access_token(token)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_security.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.security`.

- [ ] **Step 3: 의존성 추가**

`pyproject.toml`의 `dependencies` 목록에 추가:
```toml
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
```

Run: `uv sync`
Expected: 두 패키지 설치 완료.

- [ ] **Step 4: app/auth/security.py 구현**

`app/auth/__init__.py`는 빈 파일. `app/auth/security.py`:
```python
from datetime import datetime, timedelta, timezone

import jwt
from argon2 import PasswordHasher
from argon2.exceptions import VerifyMismatchError

from app.config import get_settings

ACCESS_TOKEN_MINUTES = 30

_hasher = PasswordHasher()


def hash_password(password: str) -> str:
    return _hasher.hash(password)


def verify_password(password: str, password_hash: str) -> bool:
    try:
        return _hasher.verify(password_hash, password)
    except VerifyMismatchError:
        return False


def create_access_token(user_id: int, role: str) -> str:
    # JWT의 iat/exp는 실제 UTC 절대시각이어야 한다(PyJWT가 naive datetime을
    # UTC로 간주해 처리). 프로젝트의 로컬시간 저장 규칙(now_local())과는 별개.
    now = datetime.now(timezone.utc)
    payload = {
        "sub": str(user_id),
        "role": role,
        "iat": now,
        "exp": now + timedelta(minutes=ACCESS_TOKEN_MINUTES),
    }
    return jwt.encode(payload, get_settings().jwt_secret, algorithm="HS256")


def decode_access_token(token: str) -> dict:
    return jwt.decode(token, get_settings().jwt_secret, algorithms=["HS256"])
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `uv run pytest tests/test_security.py -v`
Expected: PASS (5개 테스트).

- [ ] **Step 6: 커밋**

```bash
git add pyproject.toml uv.lock app/auth/__init__.py app/auth/security.py tests/test_security.py
git commit -m "기능: 비밀번호 해시(argon2) 및 JWT 액세스 토큰 유틸 추가"
```

---

### Task 3: RefreshToken 모델 & 회전 유틸

**Files:**
- Create: `app/models/refresh_token.py`
- Modify: `app/models/__init__.py`
- Create: `app/queries/refresh_tokens.sql`
- Modify: `app/auth/security.py`
- Modify: `tests/test_alembic_migration.py`
- Test: `tests/test_refresh_token.py`

**Interfaces:**
- Consumes: `app.models.base.BaseEntity`, `app.models.user.User`, `app.db.raw_connection`.
- Produces:
  - `app.models.refresh_token.RefreshToken` (table=`refresh_tokens`). 컬럼: `user_id: int`(FK `users.id`), `token_hash: str`(unique, index), `expires_at: datetime`, `revoked_at: datetime | None` + `BaseEntity`.
  - `app.auth.security.REFRESH_TOKEN_DAYS: int = 14`
  - `app.auth.security.generate_refresh_token() -> str`
  - `app.auth.security.hash_refresh_token(token: str) -> str`
  - aiosql 쿼리(`app.queries.queries`): `insert_refresh_token(conn, user_id, token_hash, expires_at, created_at, updated_at) -> int`, `find_by_token_hash(conn, token_hash) -> Record | None`, `revoke_by_id(conn, id, revoked_at, updated_at)`, `revoke_all_for_user(conn, user_id, revoked_at, updated_at)`.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_refresh_token.py`:
```python
from datetime import timedelta

from app.db import raw_connection
from app.models.refresh_token import RefreshToken
from app.models.user import User
from app.queries import queries
from app.utils.time import now_local


async def _make_user(db_session, email: str) -> User:
    user = User(email=email, password_hash="hashed-value")
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)
    return user


async def test_refresh_token_model_defaults(db_session):
    user = await _make_user(db_session, "rt-model@example.com")

    token = RefreshToken(
        user_id=user.id, token_hash="model-hash-1", expires_at=now_local() + timedelta(days=14)
    )
    db_session.add(token)
    await db_session.commit()
    await db_session.refresh(token)

    assert isinstance(token.id, int)
    assert token.revoked_at is None


async def test_insert_and_find_refresh_token_by_hash(db_session):
    user = await _make_user(db_session, "rt-query@example.com")
    conn = await raw_connection(db_session)
    now = now_local()

    token_id = await queries.insert_refresh_token(
        conn,
        user_id=user.id,
        token_hash="query-hash-1",
        expires_at=now + timedelta(days=14),
        created_at=now,
        updated_at=now,
    )
    assert isinstance(token_id, int)

    row = await queries.find_by_token_hash(conn, token_hash="query-hash-1")
    assert row["user_id"] == user.id
    assert row["revoked_at"] is None


async def test_revoke_by_id_marks_token_revoked(db_session):
    user = await _make_user(db_session, "rt-revoke@example.com")
    conn = await raw_connection(db_session)
    now = now_local()

    token_id = await queries.insert_refresh_token(
        conn,
        user_id=user.id,
        token_hash="revoke-hash-1",
        expires_at=now + timedelta(days=14),
        created_at=now,
        updated_at=now,
    )

    await queries.revoke_by_id(conn, id=token_id, revoked_at=now, updated_at=now)

    row = await queries.find_by_token_hash(conn, token_hash="revoke-hash-1")
    assert row["revoked_at"] is not None


async def test_revoke_all_for_user_revokes_every_active_token(db_session):
    user = await _make_user(db_session, "rt-revoke-all@example.com")
    conn = await raw_connection(db_session)
    now = now_local()

    await queries.insert_refresh_token(
        conn, user_id=user.id, token_hash="all-hash-1",
        expires_at=now + timedelta(days=14), created_at=now, updated_at=now,
    )
    await queries.insert_refresh_token(
        conn, user_id=user.id, token_hash="all-hash-2",
        expires_at=now + timedelta(days=14), created_at=now, updated_at=now,
    )

    await queries.revoke_all_for_user(conn, user_id=user.id, revoked_at=now, updated_at=now)

    row1 = await queries.find_by_token_hash(conn, token_hash="all-hash-1")
    row2 = await queries.find_by_token_hash(conn, token_hash="all-hash-2")
    assert row1["revoked_at"] is not None
    assert row2["revoked_at"] is not None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_refresh_token.py -v`
Expected: FAIL — `ModuleNotFoundError: app.models.refresh_token`.

- [ ] **Step 3: app/models/refresh_token.py 구현**

```python
from datetime import datetime
from typing import Optional

from sqlmodel import Field

from app.models.base import BaseEntity


class RefreshToken(BaseEntity, table=True):
    __tablename__ = "refresh_tokens"

    user_id: int = Field(foreign_key="users.id", index=True)
    token_hash: str = Field(unique=True, index=True)
    expires_at: datetime
    revoked_at: Optional[datetime] = Field(default=None)
```

`app/models/__init__.py`:
```python
from app.models.base import BaseEntity
from app.models.refresh_token import RefreshToken
from app.models.user import User

__all__ = ["BaseEntity", "RefreshToken", "User"]
```

- [ ] **Step 4: app/queries/refresh_tokens.sql 작성**

```sql
-- name: insert_refresh_token<!
INSERT INTO refresh_tokens (user_id, token_hash, expires_at, created_at, updated_at)
VALUES (:user_id, :token_hash, :expires_at, :created_at, :updated_at)
RETURNING id;

-- name: find_by_token_hash^
SELECT id, user_id, token_hash, expires_at, revoked_at, created_at, updated_at
FROM refresh_tokens
WHERE token_hash = :token_hash;

-- name: revoke_by_id!
UPDATE refresh_tokens
SET revoked_at = :revoked_at, updated_at = :updated_at
WHERE id = :id;

-- name: revoke_all_for_user!
UPDATE refresh_tokens
SET revoked_at = :revoked_at, updated_at = :updated_at
WHERE user_id = :user_id AND revoked_at IS NULL;
```

- [ ] **Step 5: app/auth/security.py에 회전 유틸 추가**

`app/auth/security.py` 맨 위 import에 추가:
```python
import hashlib
import secrets
```

파일 끝에 추가:
```python
REFRESH_TOKEN_DAYS = 14


def generate_refresh_token() -> str:
    return secrets.token_urlsafe(48)


def hash_refresh_token(token: str) -> str:
    return hashlib.sha256(token.encode()).hexdigest()
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `uv run pytest tests/test_refresh_token.py tests/test_security.py -v`
Expected: PASS.

- [ ] **Step 7: Alembic 마이그레이션 생성 & 적용**

Run: `uv run alembic revision --autogenerate -m "create refresh_tokens table"`
Expected: `refresh_tokens` 테이블 create + `user_id` → `users.id` FK 포함.

Run: `uv run alembic upgrade head`
Expected: 에러 없이 적용.

`tests/test_alembic_migration.py`의 assert 블록에 한 줄 추가:
```python
    assert "refresh_tokens" in tables
```

Run: `uv run pytest tests/test_alembic_migration.py -v`
Expected: PASS.

- [ ] **Step 8: 커밋**

```bash
git add app/models/refresh_token.py app/models/__init__.py app/queries/refresh_tokens.sql app/auth/security.py tests/test_refresh_token.py tests/test_alembic_migration.py alembic/versions/
git commit -m "기능: RefreshToken 모델과 회전용 쿼리/유틸 추가"
```

---

### Task 4: 회원가입 API (POST /auth/register)

**Files:**
- Create: `app/queries/users.sql`
- Create: `app/auth/router.py`
- Modify: `app/main.py`
- Modify: `tests/conftest.py`
- Test: `tests/test_auth_register.py`

**Interfaces:**
- Consumes: `app.auth.security.hash_password`, `app.db.get_db`, `app.db.raw_connection`, `app.utils.errors.Errors`, `app.utils.time.now_local`.
- Produces:
  - aiosql 쿼리: `find_by_email(conn, email) -> Record | None`, `insert_user(conn, email, password_hash, role, status, created_at, updated_at) -> int`.
  - `app.auth.router.router` (FastAPI `APIRouter`, prefix `/auth`). 라우트: `POST /auth/register` → 201 `{"id": int, "status": "pending"}`, 이메일 중복 시 409.
  - `tests/conftest.py`의 `client` 픽스처(테스트 전용, `get_db`를 `db_session`으로 오버라이드).

- [ ] **Step 1: conftest에 client 픽스처 추가**

`tests/conftest.py` 맨 위 import에 추가:
```python
import pytest_asyncio
from httpx import ASGITransport, AsyncClient

from app.db import get_db
```

파일 끝에 추가:
```python
@pytest_asyncio.fixture
async def client(db_session):
    async def _override_get_db():
        yield db_session

    from app.main import app

    app.dependency_overrides[get_db] = _override_get_db
    transport = ASGITransport(app=app, raise_app_exceptions=False)
    async with AsyncClient(transport=transport, base_url="http://test") as c:
        yield c
    app.dependency_overrides.clear()
```

> `app.main`을 함수 안에서 import하는 이유: `tests/test_health.py`가 이미 자체 `client` 픽스처(모듈 로컬)를 갖고 있어 충돌하지 않지만, 다른 테스트 파일이 모듈 최상단에서 `app.main`을 import하면 아직 정의되지 않은 라우터를 참조하다 순환 import가 생길 수 있어, 픽스처 호출 시점에 지연 import한다.

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_auth_register.py`:
```python
async def test_register_creates_pending_user(client):
    resp = await client.post(
        "/auth/register", json={"email": "new@example.com", "password": "pw12345"}
    )
    assert resp.status_code == 201
    body = resp.json()
    assert body["status"] == "pending"
    assert isinstance(body["id"], int)


async def test_register_rejects_duplicate_email(client):
    await client.post(
        "/auth/register", json={"email": "dup@example.com", "password": "pw12345"}
    )
    resp = await client.post(
        "/auth/register", json={"email": "dup@example.com", "password": "other-pw"}
    )
    assert resp.status_code == 409
    assert resp.json()["code"] == "CONFLICT"
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `uv run pytest tests/test_auth_register.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.router` (또는 404, 라우터 미등록).

- [ ] **Step 4: app/queries/users.sql 작성**

```sql
-- name: find_by_email^
SELECT id, email, password_hash, role, status, approved_at, approved_by,
       created_at, updated_at, created_by, updated_by
FROM users
WHERE email = :email;

-- name: insert_user<!
INSERT INTO users (email, password_hash, role, status, created_at, updated_at)
VALUES (:email, :password_hash, :role, :status, :created_at, :updated_at)
RETURNING id;
```

- [ ] **Step 5: app/auth/router.py 구현**

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.security import hash_password
from app.db import get_db, raw_connection
from app.queries import queries
from app.utils.errors import Errors
from app.utils.time import now_local

router = APIRouter(prefix="/auth", tags=["auth"])


class RegisterRequest(BaseModel):
    email: str
    password: str


@router.post("/register", status_code=201)
async def register(body: RegisterRequest, db: AsyncSession = Depends(get_db)):
    conn = await raw_connection(db)
    existing = await queries.find_by_email(conn, email=body.email)
    if existing is not None:
        raise Errors.conflict("이미 등록된 이메일입니다.")

    now = now_local()
    user_id = await queries.insert_user(
        conn,
        email=body.email,
        password_hash=hash_password(body.password),
        role="member",
        status="pending",
        created_at=now,
        updated_at=now,
    )
    await db.commit()
    return {"id": user_id, "status": "pending"}
```

- [ ] **Step 6: app/main.py에 라우터 등록**

`app/main.py`의 import에 추가:
```python
from app.auth.router import router as auth_router
```

`app.include_router(health_router)` 다음 줄에 추가:
```python
app.include_router(auth_router)
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `uv run pytest tests/test_auth_register.py -v`
Expected: PASS (2개 테스트).

Run: `uv run pytest -v`
Expected: 전체 PASS(회귀 없음).

- [ ] **Step 8: 커밋**

```bash
git add app/queries/users.sql app/auth/router.py app/main.py tests/conftest.py tests/test_auth_register.py
git commit -m "기능: 회원가입 API 추가 (POST /auth/register)"
```

---

### Task 5: 로그인 API (POST /auth/login)

**Files:**
- Modify: `app/auth/router.py`
- Test: `tests/test_auth_login.py`

**Interfaces:**
- Consumes: `queries.find_by_email`, `queries.insert_refresh_token`, `app.auth.security.{verify_password, create_access_token, generate_refresh_token, hash_refresh_token, ACCESS_TOKEN_MINUTES, REFRESH_TOKEN_DAYS}`, `app.config.get_settings`.
- Produces: `POST /auth/login` → 200 `{"id": int, "email": str, "role": str}` + `Set-Cookie: access_token`, `Set-Cookie: refresh_token`(둘 다 httpOnly). 실패 시 401(자격증명 불일치) 또는 403(미승인/비활성 상태).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_auth_login.py`:
```python
from app.auth.security import hash_password
from app.models.user import User


async def _create_user(db_session, email: str, password: str, status: str = "active") -> User:
    user = User(email=email, password_hash=hash_password(password), status=status)
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)
    return user


async def test_login_succeeds_for_active_user(client, db_session):
    await _create_user(db_session, "active@example.com", "pw12345")

    resp = await client.post(
        "/auth/login", json={"email": "active@example.com", "password": "pw12345"}
    )
    assert resp.status_code == 200
    assert resp.json()["email"] == "active@example.com"
    assert "access_token" in resp.cookies
    assert "refresh_token" in resp.cookies


async def test_login_rejects_wrong_password(client, db_session):
    await _create_user(db_session, "active2@example.com", "pw12345")

    resp = await client.post(
        "/auth/login", json={"email": "active2@example.com", "password": "wrong-pw"}
    )
    assert resp.status_code == 401


async def test_login_rejects_unknown_email(client):
    resp = await client.post(
        "/auth/login", json={"email": "nobody@example.com", "password": "pw12345"}
    )
    assert resp.status_code == 401


async def test_login_rejects_pending_user(client, db_session):
    await _create_user(db_session, "pending@example.com", "pw12345", status="pending")

    resp = await client.post(
        "/auth/login", json={"email": "pending@example.com", "password": "pw12345"}
    )
    assert resp.status_code == 403
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_auth_login.py -v`
Expected: FAIL — 404 (`/auth/login` 라우트 없음).

- [ ] **Step 3: app/auth/router.py에 로그인 구현 추가**

`app/auth/router.py`의 import를 다음으로 교체:
```python
from datetime import timedelta

from fastapi import APIRouter, Depends, Response
from pydantic import BaseModel
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.security import (
    ACCESS_TOKEN_MINUTES,
    REFRESH_TOKEN_DAYS,
    create_access_token,
    generate_refresh_token,
    hash_password,
    hash_refresh_token,
    verify_password,
)
from app.config import get_settings
from app.db import get_db, raw_connection
from app.queries import queries
from app.utils.errors import Errors
from app.utils.time import now_local
```

파일 끝에 추가:
```python
class LoginRequest(BaseModel):
    email: str
    password: str


def _set_auth_cookies(response: Response, access_token: str, refresh_token: str) -> None:
    secure = get_settings().secure_cookies
    response.set_cookie(
        "access_token",
        access_token,
        httponly=True,
        secure=secure,
        samesite="lax",
        max_age=ACCESS_TOKEN_MINUTES * 60,
    )
    response.set_cookie(
        "refresh_token",
        refresh_token,
        httponly=True,
        secure=secure,
        samesite="lax",
        max_age=REFRESH_TOKEN_DAYS * 24 * 60 * 60,
    )


@router.post("/login")
async def login(body: LoginRequest, response: Response, db: AsyncSession = Depends(get_db)):
    conn = await raw_connection(db)
    row = await queries.find_by_email(conn, email=body.email)
    if row is None or not verify_password(body.password, row["password_hash"]):
        raise Errors.unauthorized("이메일 또는 비밀번호가 올바르지 않습니다.")
    if row["status"] != "active":
        raise Errors.forbidden("관리자 승인 대기 중이거나 비활성화된 계정입니다.")

    access_token = create_access_token(row["id"], row["role"])
    refresh_token = generate_refresh_token()
    now = now_local()
    await queries.insert_refresh_token(
        conn,
        user_id=row["id"],
        token_hash=hash_refresh_token(refresh_token),
        expires_at=now + timedelta(days=REFRESH_TOKEN_DAYS),
        created_at=now,
        updated_at=now,
    )
    await db.commit()

    _set_auth_cookies(response, access_token, refresh_token)
    return {"id": row["id"], "email": row["email"], "role": row["role"]}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `uv run pytest tests/test_auth_login.py -v`
Expected: PASS (4개 테스트).

Run: `uv run pytest -v`
Expected: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add app/auth/router.py tests/test_auth_login.py
git commit -m "기능: 로그인 API 추가 (POST /auth/login, httpOnly 쿠키 발급)"
```

---

### Task 6: 토큰 갱신 & 로그아웃 (POST /auth/refresh, POST /auth/logout)

**Files:**
- Modify: `app/auth/router.py`
- Test: `tests/test_auth_refresh_logout.py`

**Interfaces:**
- Consumes: `queries.find_by_token_hash`, `queries.revoke_by_id`, `queries.revoke_all_for_user`, `queries.find_by_email`(로그인 헬퍼로 테스트에서 사용).
- Produces: `POST /auth/refresh` → 200 `{"id": int}` + 새 `access_token`/`refresh_token` 쿠키 발급(기존 refresh 토큰은 폐기). 이미 폐기된 토큰 재사용 시 401 + 해당 사용자의 모든 리프레시 토큰 폐기. `POST /auth/logout` → 200 `{"status": "ok"}` + 쿠키 삭제 + 리프레시 토큰 폐기.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_auth_refresh_logout.py`:
```python
from app.auth.security import hash_password
from app.models.user import User


async def _login(client, db_session, email: str, password: str = "pw12345"):
    user = User(email=email, password_hash=hash_password(password), status="active")
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)

    resp = await client.post("/auth/login", json={"email": email, "password": password})
    assert resp.status_code == 200
    return user


async def test_refresh_rotates_token_and_reissues_access(client, db_session):
    await _login(client, db_session, "refresh1@example.com")
    old_refresh = client.cookies.get("refresh_token")

    resp = await client.post("/auth/refresh")
    assert resp.status_code == 200
    new_refresh = client.cookies.get("refresh_token")
    assert new_refresh != old_refresh


async def test_refresh_reuse_of_rotated_token_revokes_all_sessions(client, db_session):
    await _login(client, db_session, "refresh2@example.com")
    old_refresh = client.cookies.get("refresh_token")

    first = await client.post("/auth/refresh")
    assert first.status_code == 200
    rotated_refresh = client.cookies.get("refresh_token")

    # 이미 회전되어 폐기된 이전 토큰을 다시 사용 시도 (탈취/재사용 시나리오)
    client.cookies.set("refresh_token", old_refresh)
    reuse_resp = await client.post("/auth/refresh")
    assert reuse_resp.status_code == 401

    # 재사용 탐지로 그 사이 발급된 최신 토큰도 함께 폐기되어야 한다
    client.cookies.set("refresh_token", rotated_refresh)
    after_breach_resp = await client.post("/auth/refresh")
    assert after_breach_resp.status_code == 401


async def test_refresh_without_cookie_returns_401(client):
    resp = await client.post("/auth/refresh")
    assert resp.status_code == 401


async def test_logout_clears_cookies_and_revokes_refresh_token(client, db_session):
    await _login(client, db_session, "logout1@example.com")
    refresh_token = client.cookies.get("refresh_token")

    resp = await client.post("/auth/logout")
    assert resp.status_code == 200

    client.cookies.set("refresh_token", refresh_token)
    reuse_resp = await client.post("/auth/refresh")
    assert reuse_resp.status_code == 401
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_auth_refresh_logout.py -v`
Expected: FAIL — 404 (`/auth/refresh`, `/auth/logout` 라우트 없음).

- [ ] **Step 3: users.sql에 find_by_id 선행 추가**

아래 refresh 구현이 사용자 조회에 쓰는 `find_by_id` 쿼리를 먼저 추가한다. `app/queries/users.sql` 끝에 추가:
```sql
-- name: find_by_id^
SELECT id, email, password_hash, role, status, approved_at, approved_by,
       created_at, updated_at, created_by, updated_by
FROM users
WHERE id = :id;
```

- [ ] **Step 4: refresh/logout 구현 추가**

`app/auth/router.py`의 import에 `Request` 추가(`from fastapi import APIRouter, Depends, Request, Response`).

파일 끝에 추가:
```python
@router.post("/refresh")
async def refresh(request: Request, response: Response, db: AsyncSession = Depends(get_db)):
    token = request.cookies.get("refresh_token")
    if not token:
        raise Errors.unauthorized()

    conn = await raw_connection(db)
    token_hash = hash_refresh_token(token)
    row = await queries.find_by_token_hash(conn, token_hash=token_hash)
    now = now_local()

    if row is None:
        raise Errors.unauthorized("유효하지 않은 토큰입니다.")
    if row["revoked_at"] is not None:
        # 이미 회전되어 폐기된 토큰이 재사용됨 → 탈취 의심, 해당 사용자의 모든 세션 폐기
        await queries.revoke_all_for_user(conn, user_id=row["user_id"], revoked_at=now, updated_at=now)
        await db.commit()
        raise Errors.unauthorized("토큰이 재사용되어 모든 세션을 종료했습니다. 다시 로그인해주세요.")
    if row["expires_at"] < now:
        raise Errors.unauthorized("토큰이 만료되었습니다.")

    user_row = await queries.find_by_id(conn, id=row["user_id"])
    if user_row is None or user_row["status"] != "active":
        raise Errors.unauthorized()

    await queries.revoke_by_id(conn, id=row["id"], revoked_at=now, updated_at=now)
    new_refresh_token = generate_refresh_token()
    await queries.insert_refresh_token(
        conn,
        user_id=user_row["id"],
        token_hash=hash_refresh_token(new_refresh_token),
        expires_at=now + timedelta(days=REFRESH_TOKEN_DAYS),
        created_at=now,
        updated_at=now,
    )
    await db.commit()

    access_token = create_access_token(user_row["id"], user_row["role"])
    _set_auth_cookies(response, access_token, new_refresh_token)
    return {"id": user_row["id"]}


@router.post("/logout")
async def logout(request: Request, response: Response, db: AsyncSession = Depends(get_db)):
    token = request.cookies.get("refresh_token")
    if token:
        conn = await raw_connection(db)
        row = await queries.find_by_token_hash(conn, token_hash=hash_refresh_token(token))
        if row is not None and row["revoked_at"] is None:
            now = now_local()
            await queries.revoke_by_id(conn, id=row["id"], revoked_at=now, updated_at=now)
            await db.commit()

    response.delete_cookie("access_token")
    response.delete_cookie("refresh_token")
    return {"status": "ok"}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `uv run pytest tests/test_auth_refresh_logout.py -v`
Expected: PASS (5개 테스트).

Run: `uv run pytest -v`
Expected: 전체 PASS.

- [ ] **Step 6: 커밋**

```bash
git add app/auth/router.py app/queries/users.sql tests/test_auth_refresh_logout.py
git commit -m "기능: 토큰 갱신/로그아웃 API 추가 (리프레시 회전 + 재사용 탐지)"
```

---

### Task 7: current_user / require_admin 의존성

**Files:**
- Create: `app/auth/dependencies.py`
- Test: `tests/test_auth_dependencies.py`

**Interfaces:**
- Consumes: `queries.find_by_id`(Task 6에서 추가됨), `app.auth.security.decode_access_token`, `app.db.{get_db, raw_connection}`, `app.utils.errors.Errors`.
- Produces:
  - `app.auth.dependencies.current_user(request: Request, db: AsyncSession = Depends(get_db)) -> dict` — 쿠키의 액세스 토큰을 검증해 사용자 레코드(dict, aiosql Record 매핑)를 반환. 실패 시 `Errors.unauthorized()`.
  - `app.auth.dependencies.require_admin(user: dict = Depends(current_user)) -> dict` — `role != "admin"`이면 `Errors.forbidden()`.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_auth_dependencies.py`:
```python
from fastapi import Depends

from app.auth.dependencies import current_user, require_admin
from app.auth.security import hash_password
from app.main import app
from app.models.user import User


async def _login(client, db_session, email: str, role: str = "member", password: str = "pw12345"):
    user = User(email=email, password_hash=hash_password(password), role=role, status="active")
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)

    resp = await client.post("/auth/login", json={"email": email, "password": password})
    assert resp.status_code == 200
    return user


async def test_current_user_rejects_missing_cookie(client):
    @app.get("/_whoami")
    async def _whoami(user: dict = Depends(current_user)):
        return {"email": user["email"]}

    resp = await client.get("/_whoami")
    assert resp.status_code == 401


async def test_current_user_returns_user_after_login(client, db_session):
    @app.get("/_whoami2")
    async def _whoami2(user: dict = Depends(current_user)):
        return {"email": user["email"]}

    await _login(client, db_session, "whoami@example.com")

    resp = await client.get("/_whoami2")
    assert resp.status_code == 200
    assert resp.json()["email"] == "whoami@example.com"


async def test_require_admin_rejects_non_admin(client, db_session):
    @app.get("/_admin_only")
    async def _admin_only(user: dict = Depends(require_admin)):
        return {"email": user["email"]}

    await _login(client, db_session, "member-only@example.com", role="member")

    resp = await client.get("/_admin_only")
    assert resp.status_code == 403


async def test_require_admin_allows_admin(client, db_session):
    @app.get("/_admin_only2")
    async def _admin_only2(user: dict = Depends(require_admin)):
        return {"email": user["email"]}

    await _login(client, db_session, "admin-user@example.com", role="admin")

    resp = await client.get("/_admin_only2")
    assert resp.status_code == 200
    assert resp.json()["email"] == "admin-user@example.com"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_auth_dependencies.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.dependencies`.

- [ ] **Step 3: app/auth/dependencies.py 구현**

```python
import jwt
from fastapi import Depends, Request
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.security import decode_access_token
from app.db import get_db, raw_connection
from app.queries import queries
from app.utils.errors import Errors


async def current_user(request: Request, db: AsyncSession = Depends(get_db)) -> dict:
    token = request.cookies.get("access_token")
    if not token:
        raise Errors.unauthorized()

    try:
        payload = decode_access_token(token)
    except jwt.InvalidTokenError:
        raise Errors.unauthorized("인증 정보가 유효하지 않습니다.")

    conn = await raw_connection(db)
    row = await queries.find_by_id(conn, id=int(payload["sub"]))
    if row is None or row["status"] != "active":
        raise Errors.unauthorized("인증 정보가 유효하지 않습니다.")
    return dict(row)


def require_admin(user: dict = Depends(current_user)) -> dict:
    if user["role"] != "admin":
        raise Errors.forbidden()
    return user
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `uv run pytest tests/test_auth_dependencies.py -v`
Expected: PASS (4개 테스트).

Run: `uv run pytest -v`
Expected: 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add app/auth/dependencies.py tests/test_auth_dependencies.py
git commit -m "기능: current_user/require_admin 의존성 추가"
```

---

### Task 8: 관리자 승인 API (GET /admin/users, approve/reject)

**Files:**
- Modify: `app/queries/users.sql`
- Create: `app/auth/admin_router.py`
- Modify: `app/main.py`
- Test: `tests/test_admin_users.py`

**Interfaces:**
- Consumes: `app.auth.dependencies.require_admin`, `queries.{find_by_id, list_by_status, update_status}`, `app.utils.errors.Errors`, `app.utils.time.now_local`.
- Produces:
  - aiosql 쿼리: `list_by_status(conn, status) -> AsyncGenerator[Record]`(다건, `async for`로 순회), `update_status(conn, id, status, approved_at, approved_by, updated_at, updated_by)`.
  - `app.auth.admin_router.router`(prefix `/admin/users`, 전 라우트 `require_admin` 가드). `GET /admin/users?status=pending` → 목록. `POST /admin/users/{id}/approve` → `status="active"`. `POST /admin/users/{id}/reject` → `status="rejected"`. 대상 없으면 404.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_admin_users.py`:
```python
from app.auth.security import hash_password
from app.models.user import User


async def _login_admin(client, db_session, email: str = "admin1@example.com") -> User:
    admin = User(
        email=email, password_hash=hash_password("pw12345"), role="admin", status="active"
    )
    db_session.add(admin)
    await db_session.commit()
    await db_session.refresh(admin)

    resp = await client.post("/auth/login", json={"email": email, "password": "pw12345"})
    assert resp.status_code == 200
    return admin


async def test_list_pending_users(client, db_session):
    await _login_admin(client, db_session)
    pending = User(email="pending-list@example.com", password_hash=hash_password("pw12345"))
    db_session.add(pending)
    await db_session.commit()

    resp = await client.get("/admin/users", params={"status": "pending"})
    assert resp.status_code == 200
    emails = [row["email"] for row in resp.json()]
    assert "pending-list@example.com" in emails


async def test_approve_user_sets_status_active(client, db_session):
    admin = await _login_admin(client, db_session, email="admin2@example.com")
    target = User(email="to-approve@example.com", password_hash=hash_password("pw12345"))
    db_session.add(target)
    await db_session.commit()
    await db_session.refresh(target)

    resp = await client.post(f"/admin/users/{target.id}/approve")
    assert resp.status_code == 200
    assert resp.json()["status"] == "active"


async def test_reject_user_sets_status_rejected(client, db_session):
    await _login_admin(client, db_session, email="admin3@example.com")
    target = User(email="to-reject@example.com", password_hash=hash_password("pw12345"))
    db_session.add(target)
    await db_session.commit()
    await db_session.refresh(target)

    resp = await client.post(f"/admin/users/{target.id}/reject")
    assert resp.status_code == 200
    assert resp.json()["status"] == "rejected"


async def test_approve_unknown_user_returns_404(client, db_session):
    await _login_admin(client, db_session, email="admin4@example.com")

    resp = await client.post("/admin/users/999999/approve")
    assert resp.status_code == 404


async def test_admin_endpoints_reject_non_admin(client, db_session):
    member = User(
        email="member-blocked@example.com", password_hash=hash_password("pw12345"),
        role="member", status="active",
    )
    db_session.add(member)
    await db_session.commit()

    resp = await client.post(
        "/auth/login", json={"email": "member-blocked@example.com", "password": "pw12345"}
    )
    assert resp.status_code == 200

    resp = await client.get("/admin/users", params={"status": "pending"})
    assert resp.status_code == 403
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_admin_users.py -v`
Expected: FAIL — 404 (`/admin/users` 라우트 없음).

- [ ] **Step 3: app/queries/users.sql에 쿼리 추가**

`app/queries/users.sql` 끝에 추가:
```sql
-- name: list_by_status
SELECT id, email, role, status, approved_at, approved_by, created_at, updated_at
FROM users
WHERE status = :status
ORDER BY created_at ASC;

-- name: update_status!
UPDATE users
SET status = :status,
    approved_at = :approved_at,
    approved_by = :approved_by,
    updated_at = :updated_at,
    updated_by = :updated_by
WHERE id = :id;
```

- [ ] **Step 4: app/auth/admin_router.py 구현**

```python
from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.auth.dependencies import require_admin
from app.db import get_db, raw_connection
from app.queries import queries
from app.utils.errors import Errors
from app.utils.time import now_local

router = APIRouter(prefix="/admin/users", tags=["admin"])


@router.get("")
async def list_users(
    status: str = Query("pending"),
    db: AsyncSession = Depends(get_db),
    admin: dict = Depends(require_admin),
):
    conn = await raw_connection(db)
    return [dict(row) async for row in queries.list_by_status(conn, status=status)]


async def _set_status(
    user_id: int, new_status: str, db: AsyncSession, admin: dict
) -> dict:
    conn = await raw_connection(db)
    row = await queries.find_by_id(conn, id=user_id)
    if row is None:
        raise Errors.not_found("사용자를 찾을 수 없습니다.")

    now = now_local()
    await queries.update_status(
        conn,
        id=user_id,
        status=new_status,
        approved_at=now,
        approved_by=admin["id"],
        updated_at=now,
        updated_by=admin["id"],
    )
    await db.commit()
    return {"id": user_id, "status": new_status}


@router.post("/{user_id}/approve")
async def approve_user(
    user_id: int,
    db: AsyncSession = Depends(get_db),
    admin: dict = Depends(require_admin),
):
    return await _set_status(user_id, "active", db, admin)


@router.post("/{user_id}/reject")
async def reject_user(
    user_id: int,
    db: AsyncSession = Depends(get_db),
    admin: dict = Depends(require_admin),
):
    return await _set_status(user_id, "rejected", db, admin)
```

- [ ] **Step 5: app/main.py에 라우터 등록**

`app/main.py`의 import에 추가:
```python
from app.auth.admin_router import router as admin_users_router
```

`app.include_router(auth_router)` 다음 줄에 추가:
```python
app.include_router(admin_users_router)
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `uv run pytest tests/test_admin_users.py -v`
Expected: PASS (6개 테스트).

Run: `uv run pytest -v`
Expected: 전체 PASS.

- [ ] **Step 7: 커밋**

```bash
git add app/queries/users.sql app/auth/admin_router.py app/main.py tests/test_admin_users.py
git commit -m "기능: 관리자 승인 API 추가 (GET /admin/users, approve/reject)"
```

---

### Task 9: 최초 admin 시드

**Files:**
- Modify: `app/config.py`
- Modify: `.env` / `.env.example`
- Create: `app/auth/seed_admin.py`
- Create: `scripts/seed_admin.py`
- Modify: `README.md`
- Test: `tests/test_seed_admin.py`

**Interfaces:**
- Consumes: `queries.{find_by_email, insert_user}`, `app.auth.security.hash_password`, `app.utils.time.now_local`.
- Produces: `app.auth.seed_admin.ensure_admin_seeded(conn, email: str, password: str) -> bool`(이미 존재하면 `False`, 새로 생성하면 `True`). `scripts/seed_admin.py` — `uv run python scripts/seed_admin.py`로 실행하는 CLI 진입점(`.env`의 `ADMIN_EMAIL`/`ADMIN_PASSWORD` 사용).

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_seed_admin.py`:
```python
from app.auth.seed_admin import ensure_admin_seeded
from app.db import raw_connection
from app.queries import queries


async def test_ensure_admin_seeded_creates_admin_when_missing(db_session):
    conn = await raw_connection(db_session)

    created = await ensure_admin_seeded(conn, "seed-admin@example.com", "seed-pw-12345")
    assert created is True

    row = await queries.find_by_email(conn, email="seed-admin@example.com")
    assert row is not None
    assert row["role"] == "admin"
    assert row["status"] == "active"


async def test_ensure_admin_seeded_is_idempotent(db_session):
    conn = await raw_connection(db_session)

    first = await ensure_admin_seeded(conn, "seed-admin2@example.com", "seed-pw-12345")
    second = await ensure_admin_seeded(conn, "seed-admin2@example.com", "seed-pw-12345")

    assert first is True
    assert second is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `uv run pytest tests/test_seed_admin.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.seed_admin`.

- [ ] **Step 3: app/config.py에 admin 설정 필드 추가**

`app/config.py`의 `Settings` 클래스에 다음 필드 추가(`secure_cookies` 필드 바로 아래):
```python
    admin_email: str | None = None
    admin_password: str | None = None
```

`.env`와 `.env.example`의 마지막 줄에 추가:
```
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me-to-a-strong-admin-password
```

- [ ] **Step 4: app/auth/seed_admin.py 구현**

```python
from app.auth.security import hash_password
from app.queries import queries
from app.utils.time import now_local


async def ensure_admin_seeded(conn, email: str, password: str) -> bool:
    """email의 사용자가 없으면 role=admin, status=active로 생성한다.

    이미 존재하면 아무 것도 하지 않고 False를 반환한다(재실행해도 안전).
    """
    existing = await queries.find_by_email(conn, email=email)
    if existing is not None:
        return False

    now = now_local()
    await queries.insert_user(
        conn,
        email=email,
        password_hash=hash_password(password),
        role="admin",
        status="active",
        created_at=now,
        updated_at=now,
    )
    return True
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `uv run pytest tests/test_seed_admin.py -v`
Expected: PASS (2개 테스트).

- [ ] **Step 6: scripts/seed_admin.py (CLI 진입점) 작성**

```python
import asyncio

from app.auth.seed_admin import ensure_admin_seeded
from app.config import get_settings
from app.db import async_session_maker, raw_connection


async def main() -> None:
    settings = get_settings()
    if not settings.admin_email or not settings.admin_password:
        print("ADMIN_EMAIL / ADMIN_PASSWORD가 .env에 설정되어 있지 않습니다.")
        return

    async with async_session_maker() as session:
        conn = await raw_connection(session)
        created = await ensure_admin_seeded(conn, settings.admin_email, settings.admin_password)
        await session.commit()

    if created:
        print(f"관리자 계정을 생성했습니다: {settings.admin_email}")
    else:
        print(f"이미 존재하는 계정입니다: {settings.admin_email}")


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 7: README.md에 실행 방법 추가**

`README.md`의 "개발 실행" 목록 다음(마이그레이션 단계 이후)에 항목 추가:
```markdown
5. 최초 관리자 계정 생성(최초 1회): `uv run python scripts/seed_admin.py`
```

- [ ] **Step 8: 수동 검증**

로컬 Postgres가 기동 중이고 `alembic upgrade head`가 적용된 상태에서:

Run: `uv run python scripts/seed_admin.py`
Expected: `.env`의 `ADMIN_EMAIL`로 관리자 계정 생성 메시지 출력.

Run: `uv run python scripts/seed_admin.py` (재실행)
Expected: "이미 존재하는 계정입니다" 메시지 출력(중복 생성 안 됨).

- [ ] **Step 9: 전체 테스트 실행**

Run: `uv run pytest -v`
Expected: 전체 PASS.

- [ ] **Step 10: 커밋**

```bash
git add app/config.py .env.example app/auth/seed_admin.py scripts/seed_admin.py README.md tests/test_seed_admin.py
git commit -m "기능: 최초 admin 시드 스크립트 추가 (ADMIN_EMAIL/ADMIN_PASSWORD)"
```

> `.env`는 git 미포함이므로 위 `git add` 목록에 없다 — 로컬에서 직접 `ADMIN_EMAIL`/`ADMIN_PASSWORD` 값을 채워 넣을 것.

---

## Self-Review

**1. Spec coverage** (`docs/superpowers/specs/2026-07-09-studio-design.md` "4. 인증" + "5. 통합 UI 구조" 중 백엔드 범위):
- User 모델(이메일/해시/role/status/approved_at/approved_by) → Task 1 ✓
- 자율 가입 + 관리자 승인 흐름(POST /auth/register, GET /admin/users?status=pending, approve/reject) → Task 4, 8 ✓
- 로그인 상태 검사(active/pending/rejected/disabled) → Task 5 ✓ (disabled는 status 값으로 존재하며 active가 아니면 전부 403으로 처리 — pending/rejected/disabled 모두 동일 취급, 스펙에 상태별 다른 메시지 요구 없음)
- httpOnly 쿠키 JWT, access(15~30분)+refresh(7~14일, DB 저장/회전) → Task 2, 3, 5, 6 ✓ (범위 상한 값으로 확정, Global Constraints에 명시)
- 리프레시 회전 + 탈취 재사용 탐지 → Task 6 ✓
- 로그아웃(쿠키 삭제 + DB 무효화) → Task 6 ✓
- current_user / require_admin 의존성 → Task 7 ✓
- 최초 admin 시드(.env ADMIN_EMAIL/PASSWORD) → Task 9 ✓
- 보안 체크리스트: 비밀번호 해시(argon2) ✓, XSS(httpOnly) ✓, CSRF(SameSite=Lax) ✓, 계정 열거 방지(동일 실패 메시지) ✓, 토큰 위조 방지(서버 시크릿 서명) ✓, 권한 상승 방지(current_user/require_admin 강제) ✓. **무차별 대입 방어(rate limit)는 의도적으로 이번 Plan 범위 밖**(Global Constraints에 명시, 후속 보안 강화 Plan으로 이월).
- **이번 Plan 범위 밖(후속 Plan으로 이월):** 프론트엔드(React 사이드바 role 가드, 로그인/가입 화면), `/admin/projects`·`/admin/system`(Project/Stage 등 아직 없음), 이메일 알림(스펙에도 "미래 확장"으로 명시).

**2. Placeholder scan:** "TBD/적절히 처리/자세한 건 나중에" 등 없음. 모든 코드 스텝에 실행 가능한 완전한 코드 포함 ✓.

**3. Type consistency:**
- `hash_password`/`verify_password`/`create_access_token`/`decode_access_token`(Task 2) ↔ `router.py`(Task 4·5)·`dependencies.py`(Task 7)에서 동일 시그니처로 사용 ✓.
- `generate_refresh_token`/`hash_refresh_token`/`REFRESH_TOKEN_DAYS`(Task 3) ↔ `router.py`(Task 5·6)에서 동일하게 사용 ✓.
- aiosql 쿼리 이름·파라미터: `find_by_email(conn, email)`(Task 4 정의) ↔ Task 5·9에서 동일 사용, `insert_user(conn, email, password_hash, role, status, created_at, updated_at)`(Task 4) ↔ Task 9(`seed_admin`)에서 동일 사용, `find_by_id(conn, id)`(Task 6에서 선행 추가) ↔ Task 7·8에서 동일 사용, `list_by_status`/`update_status`(Task 8) 시그니처 일관 ✓.
- **주의(실행자용):** aiosql의 다건 조회(접미사 없는 쿼리, 예 `list_by_status`)는 **async generator**를 반환한다(`await`가 아니라 `async for`로 순회, 또는 `[dict(r) async for r in ...]`). 단건(`^`)과 INSERT...RETURNING(`<!`)은 일반 awaitable이다. Task 8 Step 4의 `list_users`가 이 패턴을 따른다 — 다른 다건 쿼리를 추가할 때도 동일하게 처리할 것.
- `RefreshToken.user_id`/`refresh_tokens.sql`의 `user_id` 컬럼명 일관 ✓. `User.approved_by`/`created_by`/`updated_by`가 전부 `foreign_key="users.id"`로 일관 선언됨(Task 1) ✓.

**주의 노트(실행자용):**
- Task 1·3의 Alembic autogenerate(Step 5/7)는 로컬 Postgres가 필요하다 — Plan 1에서 구성한 `docker compose up -d db`를 먼저 기동해둘 것.
- 테스트는 Docker 데몬 필요(testcontainers, 기존과 동일).
- Task 6은 Step 3(`find_by_id` 쿼리 추가)을 Step 4(refresh/logout 구현)보다 먼저 적용해야 한다(순서 유의, 이미 그 순서로 작성됨).
